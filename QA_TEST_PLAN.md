# 영어 듣기 숙제 앱 테스트 계획

작성일: 2026-07-25  
대상: `english-listening-homework.html`

## 자동 점검

### 정적 점검

- HTML 내 JavaScript를 추출해 `node --check`로 문법을 검사한다.
- `git diff --check`로 공백·패치 오류를 검사한다.
- `node --test tests/learning-state.test.mjs`로 다음 학습 상태 규칙을 검사한다.
  - 목표 횟수 `0`인 요일은 재생·진행률·완주 판정에서 제외된다.
  - 평일 듣기와 주간 과제가 합산되어 진행률이 계산된다.
  - 활성 학습 항목이 없을 때는 주간 완주 배너가 표시되지 않는다.
  - 월요일부터 금요일까지 오늘 요일 표시가 정확하다.

### 브라우저 자동 점검

개발용 브라우저 테스트 도구를 사용해, 빈 브라우저 저장소에서 아래 흐름을 점검한다.

1. 첫 접속 시 엄마 설정 화면이 보인다.
2. 유효한 구간·목표를 입력한 뒤 설정 완료를 누르면 아이 화면으로 전환된다.
3. 목표가 1 이상인데 구간이 잘못된 경우 설정 화면의 오류 안내가 보인다.
4. 앱 이름 또는 버전이 맞지 않는 JSON 백업을 고르면 현재 설정을 바꾸지 않고 오류 안내가 보인다.
5. 아이 화면에는 JSON 백업 저장·불러오기 버튼이 없다.
6. 엄마 설정 화면에는 백업 저장·불러오기와 새 주 시작 기능이 있다.
7. 새 주 시작 확인을 거치면 횟수·과제 체크·포인트만 초기화된다.

## iPad Safari 수동 점검

### 오디오와 저장

1. Files에서 MP3를 선택할 수 있다.
2. `이 기기에 오디오 저장`을 켠 뒤 페이지를 다시 열었을 때 자동 연결을 시도한다.
3. 저장본이 없을 때만 아이 화면에 오디오 재선택 UI가 보인다.
4. MP3가 구간 끝보다 짧으면 횟수가 올라가지 않고 안내가 보인다.

### 아이 학습 흐름

1. 재생 버튼을 누르면 설정한 시작 시각부터 재생된다.
2. 재생 중 버튼에는 구간 진행률이 보인다.
3. 같은 버튼으로 일시정지와 재개가 가능하다.
4. 설정된 종료 지점에 도달했을 때만 횟수가 1 증가한다.
5. 목표 달성 뒤에는 재생 버튼 대신 저장된 완료 문구가 보인다.
6. 목표가 `0`인 요일은 `오늘은 쉬는 날 ☁️` 카드로 보이며 재생 버튼이 없다.
7. 오늘 요일의 카드는 `오늘` 배지와 강조 테두리로 보인다.
8. 모든 활성 항목을 끝내면 주간 완주 배너가 보이고, 시나모롤 테마에서만 시나모롤 이미지가 함께 보인다.

### 보호자 관리 흐름

1. 암호가 설정된 경우 아이 화면에서 설정으로 돌아가려면 암호가 필요하다.
2. 아이 화면에서 백업 기능을 찾을 수 없다.
3. 엄마 설정 화면에서만 JSON 백업 저장·불러오기가 가능하다.
4. 잘못된 백업 파일을 불러와도 기존 설정과 진도가 남아 있다.
5. 홈 화면에 추가한 뒤에도 아이콘과 앱 제목이 의도한 대로 보인다.

## 판정 기준

- P1 항목 하나라도 실패하면 배포하지 않는다.
- iPad에서 MP3 선택 또는 실제 구간 재생이 실패하면 데스크톱 통과만으로 배포하지 않는다.
- 배포 전에는 사용자에게 로컬 결과를 먼저 보여주고, 배포 승인 여부를 다시 확인한다.
