# 영어 듣기 숙제 서비스 개요

> 현재 구현을 기준으로 한 상세 기능 명세와 다음 재구축 계획은 [v0.1 기능 명세와 v0.2 재구축 계획](docs/V0.1_FUNCTION_SPEC_AND_V0.2_REBUILD_PLAN.md)을 참고하세요. 이 문서는 서비스 배경과 제품 방향을 중심으로 유지합니다.

## 1. 서비스 한눈에 보기

이 서비스는 아이가 정해진 영어 오디오 구간을 반복해서 듣고, 주간 과제를 스스로 확인할 수 있도록 만든 **가정용 영어 듣기 학습표 웹앱**입니다. 보호자는 한 주의 학습 계획을 설정하고, 아이는 학습 화면에서 재생·진행·완료 여부만 간단히 확인합니다.

별도의 회원가입, 서버, 데이터베이스 없이 하나의 HTML 파일을 브라우저에서 열어 사용할 수 있습니다. iPad Safari의 홈 화면에 추가해 앱처럼 실행할 수 있으며, GitHub Pages로 배포하면 기기마다 같은 주소로 접근할 수 있습니다.

### 해결하려는 문제

- 매일 같은 오디오를 몇 번 들었는지 아이와 보호자가 쉽게 확인하기 어렵다.
- 오디오 전체가 아닌, 숙제에 필요한 특정 구간만 반복 재생하고 싶다.
- 평일 듣기와 주간 과제를 한 화면에서 관리하고 싶다.
- 아이에게는 단순하고 즐거운 화면을, 보호자에게는 유연한 설정 화면을 제공하고 싶다.
- iPad 중심의 가정 환경에서 복잡한 계정·DB 설정 없이 주간 학습 기록을 이어가고 싶다.

---

## 2. 사용자 측면

### 2.1 사용자 역할

| 사용자 | 주요 목적 | 사용하는 화면 |
| --- | --- | --- |
| 보호자(엄마) | 이번 주 학습 계획, 오디오, 과제, 문구, 보상 규칙을 설정 | 엄마 설정 화면 |
| 아이 | 정해진 구간 듣기, 반복 횟수 달성, 주간 과제 체크 | 아이 학습 화면 |

보호자는 설정 화면으로 돌아갈 때 선택적으로 암호를 요구할 수 있습니다. 아이 화면에서는 설정 입력란이 노출되지 않습니다.

### 2.2 보호자 설정 흐름

1. 오디오 파일(MP3)을 선택합니다.
2. 필요하면 `이 기기에 오디오 저장`을 켭니다. 선택한 파일은 해당 브라우저의 기기 저장 공간에 보관되어 다음 접속 시 자동 복원을 시도합니다.
3. 월요일부터 금요일까지 각 요일의 듣기 시작·끝 시간과 목표 반복 횟수를 입력합니다.
4. 월요일 설정이 동일하다면 `월요일 설정을 화~금에 적용` 버튼으로 한 번에 복사합니다.
5. 주간 과제의 이름을 편집하고, 과제를 추가하거나 삭제합니다.
6. 평일 듣기와 주간 과제의 포인트, 마감 시각·요일을 정합니다. 포인트를 `0`으로 두면 관련 안내와 보상 문구가 아이 화면에 나타나지 않습니다.
7. 아이 화면의 제목, 안내 문구, 버튼 문구, 완료 문구 등을 원하는 표현으로 바꿉니다.
8. 필요하면 엄마 설정 진입 암호를 설정합니다.
9. `설정 완료 · 아이 학습 화면 열기`를 누르면 아이 화면으로 전환됩니다.

### 2.3 아이 학습 경험

아이 화면은 월~금 듣기 카드와 주간 과제 카드로 이루어진 2열 그리드입니다. 화면의 주요 정보는 다음과 같습니다.

- 전체 진행률: 평일 5일과 주간 과제 항목 수를 합산해 완료 수를 표시합니다.
- 요일별 카드: 현재 들은 횟수와 목표 횟수, 듣기 구간, 남은 횟수를 보여 줍니다.
- 재생 버튼: 예를 들어 목표가 3회이고 아직 한 번도 듣지 않았다면 `오디오 듣기. 3번 남았어요.`처럼 표시됩니다.
- 실시간 구간 진행률: 재생 중 버튼 배경이 실제 재생 위치에 맞춰 채워지고, `일시정지 · 42%`처럼 표시됩니다.
- 주간 과제 카드: 과제별 체크박스를 제공합니다.
- 포인트: 포인트가 설정된 경우에만 누적 포인트와 적립 조건을 보여 줍니다.

### 2.4 듣기 완료 규칙

듣기 횟수는 버튼을 누르는 즉시 올라가지 않습니다. 오디오가 설정된 시작 시각부터 끝 시각까지 실제로 재생된 경우에만 1회 증가합니다.

- 재생 중 같은 버튼을 누르면 일시정지 또는 계속 듣기로 동작합니다.
- 다른 요일의 구간은 동시에 재생할 수 없습니다.
- 오디오가 설정된 끝 시각보다 먼저 종료되면 횟수를 올리지 않고 더 긴 파일을 선택하라는 안내를 표시합니다.
- 목표 횟수를 모두 채우면 해당 카드에는 완료 문구가 보이고 재생 버튼은 사라집니다.
- 목표 횟수 `0`은 허용됩니다. 해당 요일은 `오늘은 쉬는 날`로 보이며, 진행률·포인트·완료 판정에서 제외됩니다.

### 2.5 동기부여와 포인트

평일 듣기와 주간 과제에 서로 다른 보상 규칙을 둘 수 있습니다.

- 평일 듣기 포인트는 **해당 요일**에 설정한 마감 시각 전 목표를 처음 달성했을 때 적립됩니다.
- 주간 과제 포인트는 모든 주간 과제를 처음 완료한 시점이 설정한 마감 요일·시각 전일 때 적립됩니다.
- 주간 과제 마감 요일은 일요일부터 토요일까지 선택할 수 있습니다.
- 완료 순간에는 설정된 완료 문구 목록 중 한 문장이 무작위로 선택되어 그 요일 카드에 저장됩니다. 따라서 다시 화면을 열어도 그날 받은 칭찬 문구는 유지됩니다.

기본 완료 문구는 다음과 같은 톤으로 준비되어 있습니다.

- 🎉 오늘 목표 달성! 정말 멋져!
- 🎧 집중해서 끝까지 들었네, 최고!
- 🌱 한 걸음 더 성장했어!
- 💪 꾸준히 해내는 너, 정말 대단해!
- ✅ 오늘의 영어 미션 클리어!
- 📚 네가 해낸 만큼 실력이 자라고 있어!
- 🏅 끝까지 포기하지 않은 너를 칭찬해!
- ✨ 오늘도 반짝반짝 노력했어!
- 🏆 영어 듣기 챔피언! 정말 잘했어!
- 🌈 내일도 너의 멋진 도전을 응원할게!

### 2.6 테마와 홈 화면 사용

보호자는 다음 세 가지 테마 중 하나를 선택할 수 있습니다. 선택은 엄마 설정과 아이 화면 모두에 적용됩니다.

| 테마 | 특징 |
| --- | --- |
| A · 구름 위 학습표 | 하늘색과 둥근 카드로 만든 친근한 기본 테마 |
| B · 조용한 집중 노트 | 아이보리·네이비 중심의 차분한 플래너 테마 |
| C · 오늘의 미션 보드 | 짙은 남색과 민트 포인트의 또렷한 게임형 테마 |

`manifest.webmanifest`, Apple Touch Icon, 일반 앱 아이콘을 포함하여 iPad/iPhone Safari의 `홈 화면에 추가` 동작에 대응합니다.

### 2.7 한 주가 끝난 뒤

보호자 설정 화면의 `새 주 시작` 버튼을 누르면 확인 창이 열립니다. 승인하면 다음 데이터만 초기화됩니다.

- 월~금 듣기 횟수
- 월~금 적립 포인트와 완료 시각
- 요일별 완료 문구
- 주간 과제 체크 상태
- 주간 과제 적립 포인트와 완료 시각

반대로 다음 데이터는 유지됩니다.

- 듣기 구간과 목표 횟수
- 주간 과제 이름
- 테마
- 사용자 문구
- 포인트 규칙
- 엄마 설정 암호
- 오디오 저장 설정

---

## 3. 데이터 보관, 백업, 복원

### 3.1 브라우저 내부 저장

이 서비스는 서버 DB를 사용하지 않습니다. 학습 설정과 진행 기록은 현재 브라우저의 `localStorage`에 JSON으로 저장됩니다. 따라서 같은 브라우저·같은 사이트 주소에서 새로고침하거나 다시 접속해도 설정, 반복 횟수, 체크 상태, 포인트는 유지됩니다.

선택한 MP3를 `이 기기에 오디오 저장`으로 저장한 경우에는 브라우저의 `IndexedDB`에 파일 자체를 보관합니다. 앱을 다시 열었을 때 이 저장본을 찾아 자동 연결을 시도합니다.

### 3.2 MP3 파일과 브라우저 제한

브라우저는 보안상 사용자가 Files에서 선택한 파일 경로를 웹페이지가 임의로 저장하거나 다시 열 수 없게 합니다. 따라서 백업에는 MP3 자체나 실제 경로가 아닌 다음 정보만 들어갑니다.

- 파일명

기기에 오디오 저장을 켜면 자동 복원 가능성이 높아지지만, 다음 상황에서는 다시 파일을 선택해야 할 수 있습니다.

- Safari 웹사이트 데이터 삭제
- 브라우저 저장 공간 정리 또는 iPad 저장 공간 부족
- 다른 기기·다른 브라우저에서 접속
- 파일 저장 중 브라우저 권한 또는 용량 문제가 발생한 경우

오디오가 연결되지 않은 경우에만 아이 화면에 오디오 선택 안내가 나타납니다.

### 3.3 JSON 백업과 복원

엄마 설정 화면과 아이 화면 모두에서 JSON 백업을 저장하거나 불러올 수 있습니다.

백업 파일명에는 종류와 생성 시각이 포함됩니다.

```text
영어듣기-엄마설정-YYYY-MM-DD_HH-MM-SS.json
영어듣기-학습진도-YYYY-MM-DD_HH-MM-SS.json
```

백업에는 설정, 과제, 진행 기록, 포인트, 문구, 테마, 암호, MP3 파일명이 포함됩니다. MP3 파일 자체는 포함되지 않으므로 복원 후에는 필요에 따라 MP3를 다시 선택해야 합니다.

---

## 4. 개발 측면

### 4.1 프로젝트 구성

이 프로젝트는 빌드 과정이나 외부 패키지 없이 동작하는 정적 웹앱입니다.

| 파일 | 역할 |
| --- | --- |
| `english-listening-homework.html` | 화면, 스타일, 자바스크립트 로직을 모두 포함하는 핵심 단일 파일 |
| `index.html` | 핵심 파일로 자동 이동시키는 진입 페이지 |
| `manifest.webmanifest` | 홈 화면 추가 및 웹앱 메타데이터 |
| `assets/cinnamoroll-sky.png` | 기본 시나모롤 테마의 헤더 배경 |
| `assets/apple-touch-icon.png` | Apple 홈 화면 아이콘 |
| `assets/app-icon-512.png` | 일반 웹앱 아이콘 |

### 4.2 애플리케이션 구조

```text
브라우저
│
├─ index.html
│   └─ english-listening-homework.html 로 이동
│
└─ english-listening-homework.html
    ├─ 엄마 설정 화면
    ├─ 아이 학습 화면
    ├─ 암호/새 주 시작 확인 모달
    ├─ 오류 안내 화면
    ├─ localStorage: 설정·진도·포인트·문구
    ├─ IndexedDB: 선택적으로 저장한 MP3 Blob
    └─ HTMLAudioElement: 설정 구간 재생
```

코드는 즉시 실행 함수(IIFE) 안에 배치되어 전역 변수 오염을 줄입니다. DOM 요소는 `id` 기반 헬퍼 함수로 찾고, 화면 전환 때마다 현재 상태를 바탕으로 다시 렌더링합니다.

### 4.3 상태 모델

핵심 상태는 `englishListeningHomeworkV1` 키로 `localStorage`에 저장됩니다. 개념적으로 다음과 같은 구조입니다.

```js
{
  screen: 'setup' | 'child',
  theme: 'cinnamoroll' | 'light' | 'dark',
  days: [
    {
      name: '월요일',
      segment: '10:11-10:20',
      target: '3',
      count: 1,
      points: 200,
      completedAt: 'ISO 시간 또는 빈 문자열',
      celebration: '목표 달성 때 선택된 문구'
    }
  ],
  tasks: ['영어 읽기 영상 찍기'],
  taskDone: [false],
  completeMessages: ['완료 문구 1', '완료 문구 2'],
  rewards: {
    dailyPoints: 200,
    dailyDeadline: '18:00',
    weeklyPoints: 500,
    weeklyDeadlineDay: 6,
    weeklyDeadline: '12:00',
    weeklyEarned: 0,
    weeklyCompletedAt: ''
  },
  parentPassword: '',
  texts: { /* 아이 화면의 교체 가능한 문구 */ },
  audio: {
    name: '파일명.mp3',
    saveToDevice: true
  }
}
```

기존 저장 데이터는 `normalizeState()`에서 보정합니다. 오래된 기본 문구를 새 기본 문구로 옮기고, 누락된 포인트·암호·오디오·완료 문구 관련 필드에 기본값을 채워 이전 사용자의 데이터를 가능한 한 유지합니다.

### 4.4 오디오 구간 재생 구현

오디오 재생은 브라우저 기본 `HTMLAudioElement`를 사용합니다.

1. 사용자가 MP3를 선택하면 `URL.createObjectURL()`로 재생용 URL을 만듭니다.
2. 재생 버튼을 누르면 설정된 시작 시각으로 `currentTime`을 이동한 뒤 재생합니다.
3. 약 60ms 간격의 타이머와 `timeupdate` 이벤트로 실제 재생 위치를 확인합니다.
4. 설정된 종료 시각에 도달하면 재생을 멈추고 해당 요일의 `count`를 1 증가시킵니다.
5. 파일이 종료 시각보다 먼저 끝나면 완료 횟수는 증가시키지 않습니다.

구간 입력은 `분:초-분:초`와 `분:초–분:초`를 모두 허용합니다. 예: `10:11-10:20`, `10:11–10:20`. 종료 시각은 시작 시각보다 커야 합니다.

### 4.5 진행률과 보상 계산

- 전체 진행률의 분모는 `평일 5일 + 현재 주간 과제 개수`입니다.
- 평일 목표 달성 여부는 `count >= target`으로 계산합니다.
- 목표가 0인 평일은 즉시 완료로 계산됩니다.
- 평일 포인트는 현재 요일이 카드의 요일과 같고, 완료 시점이 마감 전일 때만 적립합니다.
- 주간 과제 포인트는 모든 체크박스가 선택된 최초 시점에만 판정하고 저장합니다.
- 일요일 마감은 월~토 전체와 일요일 마감 시각 전까지를 허용합니다. 월~토 마감은 해당 요일 전 또는 해당 요일 마감 시각 전만 허용합니다.

### 4.6 오류 처리와 복구

예상하지 못한 자바스크립트 오류나 Promise 오류가 발생하면 빈 화면 대신 오류 안내 화면을 보여 줍니다. 이 화면의 `설정 초기화` 버튼은 다음을 지웁니다.

- `localStorage`의 앱 상태
- IndexedDB에 저장된 현재 오디오 파일
- 현재 브라우저 세션의 오디오 URL

설정 입력 오류는 설정 화면 안의 오류 영역으로 안내합니다. 대표적인 검증 항목은 다음과 같습니다.

- 목표 반복 횟수는 0 이상의 정수여야 함
- 목표가 1 이상이면 유효한 듣기 구간이 필요함
- 주간 과제 이름은 비워 둘 수 없음
- 포인트는 0 이상이어야 함
- 마감 시간은 유효한 `HH:MM` 형식이어야 함
- 설정 암호는 비어 있거나 4자 이상이어야 함

### 4.7 보안과 개인정보 고려 사항

이 앱은 개인·가정용 로컬 도구입니다.

- 계정이나 서버 DB가 없으므로 학습 데이터가 외부 서버로 전송되지 않습니다.
- 암호는 브라우저 내부 상태에 평문으로 저장되는 간단한 화면 잠금 장치입니다. 강한 보안이나 보호자 인증 수단으로 간주하면 안 됩니다.
- GitHub Pages에 배포해도 학습 데이터와 MP3는 GitHub에 자동 업로드되지 않습니다. 설정과 오디오는 각 브라우저에 저장됩니다.
- JSON 백업 파일에는 설정과 진도가 포함되므로 공유·전송 전 내용을 확인하는 것이 좋습니다.

### 4.8 배포 방식

정적 파일만으로 구성되어 GitHub Pages에 바로 배포할 수 있습니다.

- 저장소: `d21224/sy-homework-01`
- 배포 주소: `https://d21224.github.io/sy-homework-01/`
- 실제 앱 주소: `https://d21224.github.io/sy-homework-01/english-listening-homework.html`

`index.html`이 실제 앱 파일로 이동시키므로 기본 주소로 접속해도 사용할 수 있습니다. 다만 GitHub Pages의 기본 브랜치와 Pages 설정이 활성화되어 있어야 합니다.

### 4.9 테스트 및 점검 기준

기능을 수정할 때는 최소한 아래 항목을 점검합니다.

- 자바스크립트 문법 검사
- 첫 접속 시 엄마 설정 화면 표시
- 설정 완료 후 아이 학습 화면 전환
- 오디오 미선택 시 안내와 파일 선택 UI 표시
- 잘못된 듣기 구간 또는 목표 입력 시 오류 안내
- 실제 구간 종료 후에만 횟수 증가
- 재생 중 일시정지·재개와 실시간 진행률 표시
- 목표 달성 시 재생 버튼 비노출 및 완료 문구 표시
- 새로고침 후 localStorage 상태 복원
- IndexedDB 저장 오디오의 자동 연결 시도
- JSON 백업 저장·불러오기
- 새 주 시작 시 진도만 초기화되고 설정은 유지되는지 확인
- 시나모롤·라이트·다크 테마가 두 화면 모두에 적용되는지 확인
- 모바일 화면에서 2열 카드 레이아웃과 입력 UI가 사용 가능한지 확인

---

## 5. 운영 가이드

### 매주 시작할 때

1. 엄마 설정 화면에서 MP3와 듣기 구간·반복 횟수를 확인합니다.
2. 달라진 과제, 포인트, 마감 규칙, 문구를 수정합니다.
3. 필요하면 이전 기록을 JSON으로 백업합니다.
4. 기존 설정을 그대로 쓰려면 `새 주 시작`으로 진도만 초기화합니다.
5. `설정 완료 · 아이 학습 화면 열기`를 눌러 아이에게 전달합니다.

### 기기를 바꾸거나 문제가 생겼을 때

1. 기존 기기에서 JSON 백업 파일을 저장합니다.
2. 새 기기에서 같은 앱 주소를 엽니다.
3. 백업 파일을 불러옵니다.
4. MP3 파일을 다시 선택합니다. 기기에 저장 옵션을 켜면 이후 자동 복원을 시도합니다.

### 향후 확장 아이디어

- 여러 주차의 기록을 별도로 보관하고 비교하는 학습 히스토리
- 여러 아이 프로필 전환
- 보호자용 주간 리포트와 포인트 교환표
- 오디오 파일의 파형 또는 구간 미리보기
- 더 다양한 테마·배경·아이콘 선택
- 데이터 내보내기 형식 확장(CSV 등)

---

## 6. 현재 문서의 기준

이 문서는 프로젝트의 현재 로컬 구현을 기준으로 작성했습니다. 특히 `새 주 시작`, 테마 유지, 완료 문구 무작위 표시 기능은 로컬 작업본에 포함되어 있으며, GitHub Pages에 배포되기 전이라면 실제 공개 주소에는 아직 반영되지 않을 수 있습니다.
